node_modules/
.next/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vercel

# Cursor / Claude agent worktrees (must not be git submodules — breaks Fly `git submodule` fetch)
.claude/worktrees/
.env*.local
